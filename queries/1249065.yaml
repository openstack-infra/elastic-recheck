# NOTE(mriedem): We shouldn't remove this query until kilo-eol even if
# there are no hits in 10 days as this shows up from time to time on
# stable branches before liberty. The final fix in nova in liberty contained
# an RPC API version change so will not be backported.
query: >
  message:"No nw_info cache associated with instance"
  AND tags:"screen-n-api.txt"
