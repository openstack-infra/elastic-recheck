============
Installation
============

At the command line::

    $ pip install elastic-recheck

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv elastic-recheck
    $ pip install elastic-recheck